<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TaskListMobile.Pages.TaskListIndex">
    <ContentPage.Content>
        <StackLayout>
            <Label>Task List Index Page</Label>
            <ListView 
                CachingStrategy="RecycleElement" 
                x:Name="TaskListsView" 
                ItemsSource="{Binding TaskLists}"
                ItemTapped="TaskListsView_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding Date, StringFormat='{0:MMMM dd, yyyy}'}"></Label>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Create" Command="{Binding DisplayCreateDialogCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>