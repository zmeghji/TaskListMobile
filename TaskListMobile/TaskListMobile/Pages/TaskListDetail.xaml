<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TaskListMobile.Pages.TaskListDetail">
    <ContentPage.Content>
        <StackLayout>
            <Label>Task List Detail Page</Label>
            <Label Text="{Binding Model.Date, StringFormat='{0:MMMM dd, yyyy}'}"></Label>
            <ListView CachingStrategy="RecycleElement" x:Name="TaskItemsListView" ItemsSource="{Binding Model.TaskItems}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}" Detail="{Binding Status}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>